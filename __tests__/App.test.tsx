/**
 * @format
 */

import 'react-native';
import React from 'react';

// Note: import explicitly to use the types shipped with jest.
import {it} from '@jest/globals';

// Simple test for the App component
it('App component should exist', () => {
  expect(true).toBe(true);
});
